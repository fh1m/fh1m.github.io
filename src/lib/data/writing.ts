/**
 * Writing — long-form notes from the work. Real, technical, honest. Body is an
 * array of paragraphs (strings); a leading '> ' marks a pull-quote, '## ' a
 * subhead, '- ' a bullet. Kept deliberately simple — no markdown runtime.
 */

export interface Essay {
  slug: string;
  title: string;
  dek: string;
  date: string; // YYYY-MM
  minutes: number;
  thread: string;
  tags: string[];
  body: string[];
}

export const WRITING: Essay[] = [
  {
    slug: 'the-71-transposes',
    title: 'The 71 transposes that were one operation',
    dek: 'How a feature matcher that refused to compile for a neural accelerator turned out to be a single idea wearing 71 masks — and why proving the fix was exact mattered more than the 23× speedup.',
    date: '2026-09',
    minutes: 8,
    thread: 'Perception',
    tags: ['hailo-8', 'xfeat', 'quantisation', 'breakthrough'],
    body: [
      'XFeat is a state-of-the-art lightweight feature matcher. The Hailo-8 is a neural accelerator that does 26 TOPS and nothing else well. Getting the first onto the second should have been a compile step. Instead the compiler died: `UnsupportedShuffleLayerError` on 71 Transposes, 72 Slices and 70 Unsqueezes, all five-dimensional.',
      'The temptation is to fight all 213 operations. The breakthrough was seeing that they are not 213 problems, or even 71 — they are **one**. They are `_unfold2d(x, ws=8)`, written with `Tensor.unfold`, which torch traces as a pile of 5-D slicing. That operation has a name: space-to-depth. torch already has it as `pixel_unshuffle`, and the chip supports it natively. One substitution, and the graph parses.',
      '## The proof, not the claim',
      'Here is the part I care about. A graph that compiles but computes something slightly different is the worst kind of bug: it will surface months later as a worse match rate and get blamed on quantisation, and you will never find it. So the export tool refuses to write the model until it has run `torch.equal` on random input and checked the whole network against the stock graph — max absolute difference `0.000e+00` across all three heads at both resolutions. The substitution is not an approximation. The tool proves it, or it writes nothing.',
      '> A HEF that compiles is not a result.',
      '## The honest part',
      'Then the number: 701 FPS on the chip, 23× the Pi’s CPU — a figure that appears to be published nowhere else. And immediately, the caveat that makes it true: only the backbone moved. Keypoint decoding, NMS and the matcher stay in numpy, and the match is the expensive half. So the real win is not the speed. It is that the bottleneck moved from the detector to the matcher, which reframes the next optimisation entirely. A speedup you cannot spend is not a speedup; knowing exactly where the new wall is, is.',
      'The settings that shipped came from the same discipline. On real murky footage, `top_k=1024` beats `2048` — because the similarity matrix is O(N²) in keypoints, and at a fixed budget four references at 1024 beat one at 2048. Bank breadth beats keypoint density at equal cost. Chosen, not inherited.',
    ],
  },
  {
    slug: 'a-capability-is-only-real',
    title: 'A capability is only real if the path to it is',
    dek: 'The most expensive defect I keep meeting: a feature that is written, reviewed, tested — and never runs, because nothing that runs calls it. A note on reachability as a design constraint.',
    date: '2026-08',
    minutes: 6,
    thread: 'Reliability',
    tags: ['architecture', 'reachability', 'design'],
    body: [
      'Loop closure was implemented. It had code, it had tests, it had a launch file. And it did nothing on the vehicle — because it was wired into a launch file that the documented bringup command does not include. The capability existed. The path to it did not.',
      'This is the oldest and most expensive defect in the codebase, and it has a family: the lock ladder in no launch file, a hold time held at zero, a bank storing a position nothing ever passed, a device path read and then ignored. Every one looked finished. Correct code, passing tests, nothing on the vehicle.',
      '> A capability is only real if the path to it is.',
      '## The fix is a decision, not more code',
      'The instinct is to add the missing switch — declare it in the second launch file too. That is how you get two places that must be kept in step, which is the same bug with a longer fuse. Instead the node configures itself from a single deck-editable file, the same pattern the course priors already use: module constants as defaults, an explicitly-set parameter still winning, and a test that fails if the capability ever reappears in a launch file again.',
      'And it degrades loudly. A malformed config falls back to defaults and says so — because silently ignoring an operator’s edit means they stand on the deck watching for a change that never loaded. The point is not the loop closure. The point is that reachability is a property you design in at write time, because that is the only moment anyone knows whether a consumer exists.',
    ],
  },
  {
    slug: 'the-canary-that-was-already-dead',
    title: 'The canary that was already dead',
    dek: 'Quantising a model to INT8 is supposed to cost accuracy. The question is never “does it cost some” — it is “does it push a passing case under the bar.” A note on measuring the right thing.',
    date: '2026-08',
    minutes: 6,
    thread: 'Measure',
    tags: ['quantisation', 'evaluation', 'judgment'],
    body: [
      'The plan named a canary: one detection column, `torpedo_1`, which INT8 must not push below 15 inliers. Sensible-sounding. It was also wrong, and following it would have killed a working feature.',
      'The bar was set up honestly: re-run the murky-water clip table with the chip as the backbone and everything else identical — the same post-processing, the same MAGSAC, the same 15-inlier trust threshold, the same protocol. The only variable was float32-on-CPU versus INT8-on-Hailo. Every clip scored exactly what it scored on CPU: 4/4, 3/4, 4/4, 4/4, 4/4.',
      '## Read the tell',
      'Quantisation cost a median 7.7% of inliers — and in 5 of 20 columns INT8 read *higher*, up to +40%, which is MAGSAC’s own scatter, not the chip being smarter. The number that actually decides the arm is different: **zero** passing columns crossed the bar. 19 of 20 clear 15 inliers on CPU, and the same 19 of 20 clear it on the chip.',
      '> Reporting the wrong quantity is what lets a defect survive three attempts.',
      'The canary, `torpedo_1` at 12 inliers on the chip, already read 13 on float32. It was below the bar before quantisation ever touched it. Reading its 12 as an “INT8 failure” would have condemned a working arm on a case that was already dead. The real question was whether quantisation pushed any *passing* column under — and none did. Measure the effect you actually care about, through the shipped code path, or you are measuring the harness.',
    ],
  },
  {
    slug: 'turning-a-camera-into-a-sensor',
    title: 'Turning a camera into a velocity sensor',
    dek: 'No DVL, no GPS. A downward camera and the ground can tell you how fast you are moving — if you trust the geometry more than the number.',
    date: '2026-09',
    minutes: 7,
    thread: 'Perception',
    tags: ['optical-flow', 'localization', 'underwater'],
    body: [
      'A Doppler velocity log is the obvious way to measure an underwater vehicle’s speed over ground. It is also expensive, and we did not have one. So the question became: what do we already have that can measure motion?',
      'The answer was the downward camera. If you know the height above the floor and the camera’s field of view, then optical flow — how far the texture on the floor slides between two frames — is a velocity. The geometry is simple. The trap is that every part of it can lie plausibly.',
      '## The verification that actually worked',
      'The only verification that has ever worked here is looking at the frames. We ran three 30 cm slides across a known distance and checked the implied height against a tape measure: 0.72 / 0.69 / 0.70 m against a 0.72 m tape, worst-case position error 1.09 cm. That is a real measurement — it has a method, conditions, and a bar it had to clear.',
      '> A number with no bar cannot be wrong, so it never gets checked — it just gets quoted, and eventually shipped.',
      'What makes this trustworthy is not that the estimator agreed with another estimator — that only measures agreement. It is that truth was known: a tape on the floor, a slide of a fixed length. Construct the case where you already know the answer, then see if the machine finds it.',
    ],
  },
  {
    slug: 'why-radar-doesnt-work-underwater',
    title: 'Why radar doesn’t work underwater — and what that forces',
    dek: 'Salt water eats radio in centimetres. No radio means no GPS, no radar, no telemetry link when submerged. Every design choice downstream is a consequence.',
    date: '2026-08',
    minutes: 5,
    thread: 'First principles',
    tags: ['physics', 'sensing', 'architecture'],
    body: [
      'Underwater there is no radio, no GPS, and no second chance to explain yourself. Seawater is conductive; it attenuates electromagnetic waves so fast that radar and GPS are simply off the table below the surface. This one physical fact writes most of the architecture for you.',
      'If you cannot get a position fix from the sky, you have to estimate it from the inside — dead reckoning on an IMU, corrected by whatever you can measure locally: depth from pressure, velocity from a camera, heading from the AHRS. If you cannot stream telemetry home, the vehicle has to be right on its own.',
      '> A vehicle either understood what it was looking at, or it did not — and it will not tell you which.',
      'That is why the interesting engineering is not the happy path. It is what happens when the vehicle cannot do something. The answer, on a well-built stack, is never a silent workaround that keeps the demo running. It is a loud refusal and a written reason.',
    ],
  },
  {
    slug: 'the-frame-age-problem',
    title: 'The frame-age problem',
    dek: 'A perception pipeline that looks healthy can be acting on a frame that is 400 ms old. Freshness is a feature you have to build.',
    date: '2026-07',
    minutes: 6,
    thread: 'Perception',
    tags: ['latency', 'real-time', 'vision'],
    body: [
      'When a camera feeds a queue, the consumer reads the oldest frame in the queue. Under load that frame can be hundreds of milliseconds stale — and nothing in the graph looks broken. The detector runs, the boxes appear, the counters tick up. The vehicle is just steering by the past.',
      'We measured it: a naive queue served frames 396 ms stale on average. A mailbox that keeps only the newest frame and drops the rest served them 16.9 ms stale. Same camera, same detector — the difference is entirely in which frame you choose to believe.',
      '## Freshness over throughput',
      'For a moving vehicle, an old correct answer is a wrong answer. Throughput is a benchmark number; freshness is what keeps the control loop honest. The mailbox throws away work on purpose, and that is the right trade.',
      '> The speed of light sucks. — Carmack',
      'The lesson generalises past cameras: any time a slow stage sits behind a fast one, ask what happens to the backlog. If the answer is “it queues,” you have built a time machine pointed the wrong way.',
    ],
  },
  {
    slug: 'from-worked-once-to-works-every-time',
    title: 'From “worked once” to “works every time”',
    dek: 'The gap between a demo and a competition run is the difference between a capability existing and a capability being reachable.',
    date: '2026-06',
    minutes: 6,
    thread: 'Reliability',
    tags: ['testing', 'reliability', 'process'],
    body: [
      'The most expensive defect I keep meeting is the capability that looks finished — correct code, passing tests — and does nothing on the vehicle, because nothing that runs actually calls it. A module written, reviewed, merged, and never reached.',
      '> A capability is only real if the path to it is.',
      'The fix is not more code. It is a decision made at write time: this module is either imported by something that runs, or it carries a written reason naming the consumer it is waiting for. There is no third state, and the moment you write it is the only moment anyone knows whether a consumer exists.',
      '## Injection-verify your guards',
      'A test that has never failed against a real defect is not a guard — it is decoration. So we break the guard deliberately, watch it fail, and restore it. If it does not fail when the thing it protects is broken, it was never protecting anything.',
      'Reliability is not a feeling you get from green checkmarks. It is what remains after you have tried, on purpose, to make each check lie to you.',
    ],
  },
  {
    slug: 'low-cost-vio-and-the-dvl-question',
    title: 'Low-cost VIO and the DVL question',
    dek: 'When you can’t buy the sensor, you have to earn its measurement from the ones you have. A note on fusing depth, flow and heading honestly.',
    date: '2026-05',
    minutes: 7,
    thread: 'Estimation',
    tags: ['ekf', 'sensor-fusion', 'vio'],
    body: [
      'A right-invariant EKF is the spine of the localization stack: it predicts on the board’s IMU and corrects with everything cheap we can measure — depth, optical-flow velocity, heading, and occasional fixes from recognised props.',
      'The subtle part is time. Measurements arrive late — a detection is a description of where the vehicle was when the shutter opened, not where it is now. Applying it on arrival smears the estimate. So late measurements are replayed at the instant they describe, and the filter’s history is corrected from there forward.',
      '> An absent prediction must never be read as a permissive one.',
      'Fusion is where optimism hides. A prior should stop helping exactly when it stops being one; a gate should weaken exactly when the prior stops being worth trusting. Get that backwards and the vehicle steers confidently at nothing.',
      'The DVL question — do we need the expensive sensor? — is not answered by a spec sheet. It is answered by whether the cheap estimate clears the bar the mission needs, measured in water. Until then, the honest state is written down, not assumed.',
    ],
  },
  {
    slug: 'tracking-without-detection',
    title: 'Tracking without detection',
    dek: 'Detectors blink. A lock has to survive the gap between two sightings without inventing a target that isn’t there.',
    date: '2026-04',
    minutes: 5,
    thread: 'Perception',
    tags: ['tracking', 'robustness', 'vision'],
    body: [
      'Real detectors do not fire every frame. Underwater, against murk and caustics, they drop out for stretches — we counted 71 real gaps in one run. If the controller only acts when the detector fires, the vehicle stutters and loses its target.',
      'So you build a lock ladder: a small state machine that coasts a target through detection gaps, sized against the real distribution of gaps rather than a guessed timeout. The rungs are honest about confidence — a rung that names the wrong prop confidently is worse than one that reports LOST, because LOST is honest.',
      '> The failure modes are asymmetric: a missed re-acquisition costs a second; a false one costs the run.',
      'That asymmetry sets the gates. You would rather lose the target and know it than weld two objects into one identity and have every downstream consumer believe the target teleported. Robustness here is not a bigger model — it is refusing to lie during the gap.',
    ],
  },
  {
    slug: 'the-bank-that-ran-the-backbone-five-times',
    title: 'The bank that ran the backbone five times',
    dek: 'Chasing a matcher bottleneck turned up a defect worth more than the optimisation I went looking for — a per-match cost that barely moved with `top_k`, which is the tell that something else is the real cost.',
    date: '2026-09',
    minutes: 6,
    thread: 'Perception',
    tags: ['performance', 'hailo-8', 'profiling'],
    body: [
      'The number that should not have been flat was flat: 42.3 ms per lookup at `top_k=1024`, 36.3 ms at 512, 13.3 ms at 256. A quarter of the multiply-accumulates should have cost far less than 86% of the time it was taking. When a number refuses to move with the thing that should dominate it, the thing that should dominate it isn\'t the cost — something fixed is.',
      '`Anchor.locate()` detects features internally, which is fine for one reference. But `CheckpointBank.locate()` asks several references about the *same* frame, and each one called the full detector on identical pixels. A four-wide shortlist ran the backbone four times on the same image, plus once more for the signature path — five backbone passes per lookup, four of them computing something already sitting in memory.',
      '## The fix is smaller than the bug',
      '`Anchor.locate_features(k1, d1)` now takes features already extracted, and the bank detects once and hands them to every reference. At the shipping width that took the lookup from **169.1 ms to 94.4 ms — 1.8×** — with zero accuracy cost, because the arithmetic is identical. Nothing got approximated. It just stopped being repeated.',
      '> The saving on the Hailo-8 changes too, and not the way you\'d guess.',
      'An earlier section had concluded the chip saves a fixed ~31.7 ms regardless of bank size, on the assumption that detection happens once per lookup. It doesn\'t — it happens *k+1* times — so the saving actually scales with the shortlist width. Removing the redundancy on CPU alone recovered 75 ms; the remaining chip work is what moves onto the accelerator next. The profiling number that looked boring — flat against `top_k` — was the whole finding.',
    ],
  },
  {
    slug: 'round-7-closes-no-twice',
    title: 'Round 7 closes NO — twice',
    dek: 'A fine-tune that scored a clean win on every checkpoint turned out to be scored by the wrong instrument. Retracting it once wasn\'t enough — the second retraction is the one that actually holds.',
    date: '2026-09',
    minutes: 7,
    thread: 'Measure',
    tags: ['evaluation', 'fine-tuning', 'retraction'],
    body: [
      'The first version of this result looked like a win: a 2000-step checkpoint flipped the canary column from 12 inliers to a passing score. Sweeping every checkpoint instead of reporting the best one killed it — the same canary read **15, 10, 11, 10, 10, 8** across steps 2000 through 12000, against a stock value of 11. It degrades monotonically with training. The "good" number was a lucky draw, and reporting it alone would have been the single most flattering — and wrong — number in the project.',
      'The cause traced to the training data: 60% of the set — 2,440 of 4,036 frames — was YOLO prediction *output*: footage with bounding boxes, class names and confidences already burned into the pixels. Hard edges and crisp glyphs are the easiest features in any frame and don\'t exist at inference time, so the descriptor spent its capacity learning furniture. Widened to 336 held-out pairs, median inliers read 19.0 on the untrained venue and 18.5 on the trained ones — near-identical, which is the signature of a data problem, not overfitting (overfitting would read better-on-trained, worse-on-held-out).',
      '## Round 7, take two',
      'A clean 320×240 retrain then looked like the win instead — all eight checkpoints scored at or above stock on the evaluation metric, held-out median **+17.0, +9.5, +11.0**. Run through the actual shipped ONNX path, the same weights gave **2,052 total inliers against stock\'s 2,237 — an 8.3% loss**, with the same 19-of-20 passing columns either way. No improvement. Round 7 closes NO, for real this time, and stock weights stayed.',
      '> A control arm that cannot reproduce its own recorded number is not a control arm.',
      'The instrument was the defect. The fine-tune scorer used its own keypoint extraction — top-k on the heatmap, nearest-neighbour descriptor matching — not the shipped path\'s NMS, bilinear sampling and reliability weighting. It was a different algorithm wearing the same name, and the tell was visible from the first run: the scorer\'s own stock arm reported `[210, 133, 222, 41]` where the recorded table — produced by the shipped path — reads `[189, 65, 134, 24]`. Two different numbers for one unchanged model. I noticed it, blamed post-processing, and kept using that scorer for two training runs and four checkpoint sweeps before checking. That check costs one command. Not running it cost weeks.',
    ],
  },
  {
    slug: '179-identity-switches',
    title: '179 identity switches, and why the SOTA fix costs too much',
    dek: 'A diagnostic reported "id switches: 0" for months. It was a clean-looking zero produced by a tool measuring nothing — and the real number, once measured, explained why a bigger tracker wasn\'t the answer.',
    date: '2026-09',
    minutes: 6,
    thread: 'Perception',
    tags: ['tracking', 'evaluation', 'sota'],
    body: [
      '`continuity.py` reported "id switches: 0" — a clean result, and wrong for a boring reason: it was scoring `/detections`, a topic that carries no identity field at all. Zero wasn\'t a measurement. It was the absence of one, dressed as a number. A second tool, `check_tracker.py`, made the same mistake more loudly — it read `d.tracking_id`, a field `vision_msgs/Detection2D` does not have, and raised `AttributeError` on the first track of every run. Because that code path only executes when tracks exist, the crash stayed invisible in every quiet run and every piece of documentation kept asserting the field was there.',
      'Scored correctly, on `/tracks`, with the field that actually carries identity: **179 identity switches in 253 seconds — one person, in one room.** That is the real number the project had been building on top of for however long "0" went unquestioned.',
      '## Why the obvious fix doesn\'t apply',
      'A motion tracker cannot close this gap by definition. `ByteTrack` and `OC-SORT` — the trackers actually in play here, not the more famous one people assume — associate detections on IoU plus a Kalman prediction. Across a real occlusion gap the prediction decays and the overlap disappears, so a target that returns can\'t be matched back to the track it came from. That\'s the boundary of what motion-only association can say. It isn\'t a bug in either tracker; it\'s what they\'re built to not do.',
      '> The state of the art names the fix, and also the price.',
      'BoT-SORT closes exactly this gap by fusing motion with appearance modelling and camera-motion compensation to hold identity across a break. Naming it is the easy part — it also names a cost this platform can\'t pay today: appearance re-identification at the frame rates this vehicle runs is compute the perception budget doesn\'t have free. So the honest status isn\'t "fixed" or "ignored." It\'s a priced, deferred trade: the 179-switch number stays in the README as the reason the next perception cycle should spend its budget on identity, not on a faster detector.',
    ],
  },
  {
    slug: 'the-anchor-that-learned-to-look-twice',
    title: 'The anchor that learned to take a second photograph',
    dek: 'A recognition reference frozen once looked like it was hitting a hard limit on murky footage. It wasn\'t the descriptor\'s ceiling — it was the cost of never re-photographing the thing you\'re trying to recognise.',
    date: '2026-09',
    minutes: 7,
    thread: 'Perception',
    tags: ['recognition', 'xfeat', 'debugging'],
    body: [
      'The guard read, almost apologetically, `not self._anchor.has_reference` — the anchor snapped its reference image once, the first time it saw the target, and never refreshed it again. Measured against archive clips, inliers against that one frozen reference told the whole story:',
      '`mirpur_torpedo` held 189/65/134/24 inliers at +1/+3/+5/+8 seconds. `mirpur_torpedo_1` — the murkier of the two — fell to **69/43/51/12, under the 15-inlier trust bar** at the +8s mark. Every clip\'s minimum was its own +8s column; the murkiest one crossed the failure line entirely. Run the *same frames* against a fresh reference instead of the stale one, and they score what the +1s column scored. Nothing about the descriptor changed. What changed was refusing to take a second photograph.',
      '## Composing, not replacing',
      'The fix, `CheckpointBank`, holds up to five references over one backend rather than one photo forever — and it composes `Anchor` instead of replacing it, so the ROI scaling, the homography, the pose estimate and the below-bar refusal all stay code that had already been measured on real footage. What the bank adds is only the part a single frozen photo structurally can\'t: which reference answered, which one to discard, and when the evidence says it\'s time to take a new one.',
      '> A table of inlier counts says a match is good. It never says good *at what*.',
      'Shipping the bank surfaced three more defects that no table would have shown, because `pipeline_visual_check.py` draws what the ladder actually believes — matched quad, surviving correspondences, warped annotations, identity verdict and its reason — on real footage, and looking at it for a few minutes found them. The sharpest: on close-range frames with the target filling the screen, the system refused every one at 7–8 inliers, because `CONF_FLOOR = 0.60` rejected candidate views the detector itself only scores at **p50 0.49** on this footage. A fixed floor was also the wrong shape of rule — recall on this stack swings **29.2% / 72.7% / 68.3%** across venues, so one constant can\'t be right in all three. Dropped to 0.25, and left the checks that actually discriminate — geometric agreement, eviction by yield, a 40-inlier recognition bar — to do the job a threshold was standing in for.',
    ],
  },
  {
    slug: 'scoring-an-estimator-against-a-truth-that-did-not-exist',
    title: 'Scoring an estimator against a truth that didn\'t exist yet',
    dek: 'Twenty-one tests and every one of them a unit test — because until a physics plant existed to drive against, there was no ground truth to measure drift against. Building the truth was the actual work.',
    date: '2026-09',
    minutes: 7,
    thread: 'Estimation',
    tags: ['ekf', 'simulation', 'validation'],
    body: [
      'The localization filter had 21 tests, and every single one checked a property, not a trajectory: the exp/log round-trip, covariance symmetry, positive-definiteness, the sign of one update. None of them drove the filter along a path and asked how far it had drifted from where it actually was — because until a Fossen-model plant existed, there was no "actually" to compare against. Comparing two estimators only measures whether they agree with each other. It can\'t rank them. You need a case where the true position is known by construction.',
      'The plant is that case: it produces exact position, attitude and velocity every tick, so the test harness can synthesise IMU readings, optical-flow velocity, depth and heading at their real measured rates and score the filter\'s drift against ground truth instead of against its own prior output.',
      '## The frame seam that cost −1988 degrees',
      'One convention had to be pinned rather than assumed, and getting it wrong once already produced a spectacular failure: the filter\'s world is NED-z-down with an FRD body frame, and the Fossen plant is x-forward/y-starboard/z-down. Same convention, nothing needs remapping between them — which matters, because a frame seam is exactly what produced a **−1,988-degree yaw runaway** the first time the allocator met the plant with an unpinned assumption about which frame it was reading.',
      '> Baseline, 60 seconds of a yawing trajectory with full aiding:',
      '**0.013 m position RMS · 0.31° yaw RMS · 0.002 m depth RMS.** Those three numbers are the actual finding — not because they\'re impressive in isolation, but because they\'re the first numbers in the project\'s history that mean what they appear to mean: an estimate compared to a truth, on a path complex enough that agreement wasn\'t guaranteed by construction.',
    ],
  },
  {
    slug: 'a-tighter-measurement-made-it-worse',
    title: 'A tighter measurement made the bug worse, not better',
    dek: 'Characterising exactly when a localization fix should fire turned into retracting my own conclusion from the previous commit — and finding a clean square-root law hiding in the retraction.',
    date: '2026-09',
    minutes: 6,
    thread: 'Estimation',
    tags: ['localization', 'observability', 'retraction'],
    body: [
      'The question was supposed to be simple: does *when* a landmark fix arrives matter for a known localization defect? With optical flow healthy, the answer was clean — a 2-second resection window at t=0, t=20 or t=55 all landed in the same 0.20–0.28 m band. That falls straight out of the defect\'s own mechanism: if dead-reckoning error isn\'t growing between fixes, a late one is worth exactly what an early one is worth. Timing wasn\'t the variable. Count was.',
      '## The table that mattered',
      'Instead of *when*, the real lever was *how many*:',
      '`0.5 s → 1 fix → 0.383 m (2.6× better than none)` · `5 s → 10 fixes → 0.293 m (3.4×)` · `20 s → 40 fixes → 0.139 m (7.1×)` · `continuous → 120 fixes → 0.055 m (17.9×)`',
      'That\'s close to a square-root law — error falling roughly as 1/√N — which means one resection anywhere captures most of the benefit, and every fix after that averages the estimate down rather than correcting a specific moment. It reframes mission design: it doesn\'t matter *where* the vehicle happens to sight a recognisable landmark. It matters that it keeps sighting one for *longer*.',
      '> The previous commit said the opposite, in my own words: "a fix early is worth far more than a fix often." That claim is retracted here — it was an inference, not a measurement, and the measurement disagreed with it.',
      'The retraction is the actual content of this entry, not a footnote to it. Two commits, from the same author, about the same defect, on consecutive days — one wrong, one measured. Nothing about the codebase changed between them except which claim was allowed to keep standing.',
    ],
  },
  {
    slug: 'the-sign-flip-at-the-mavlink-boundary',
    title: 'The sign flip that nearly sent it to +20 meters',
    dek: 'Closing the depth loop for the first time surfaced the largest standing unknown in the project — and a boundary where two correct conventions, read together, meant the opposite of either alone.',
    date: '2026-09',
    minutes: 6,
    thread: 'Control',
    tags: ['firmware', 'control-loop', 'debugging'],
    body: [
      'The largest unresolved warning in the project\'s own documentation read: "the depth loop has never run closed." Every mission primitive closes it implicitly — even a plain forward move — and an in-air test proves nothing, because at roughly zero metres, target and measurement already agree by default. The only real test was against a dynamics plant with actual feedback, for the first time the depth-control firmware had ever run against anything but open air.',
      '## Two correct conventions, read together, wrong',
      'Three attempts flew the simulated hull to **+20 metres** before the cause surfaced. Inside the firmware, depth is positive metres *down* — the setpoint is clamped non-negative, and a SURFACE command sets it to zero. But the vehicle\'s telemetry channel reports depth as negative *below* the surface. Both conventions are internally correct. Neither function was wrong on its own. Passing one straight into a caller that expected the other read as "the vehicle is 20 metres above the water" to a controller that then tried to make that true.',
      'Fixed, the loop closes at **17 mm of steady-state error at 0.5 m, 1.5 m and 3.0 m setpoints** — three different targets, the same tight residual. The integral term earns its place in that number: a real hull is never exactly neutrally buoyant, and against ±2 N of standing buoyancy error, the loop still settles at exactly 1.500 m with 0.000 m measured error.',
      '> The mission number underneath the steady-state one is a distance, not a percentage.',
      'Overshoot behaves the way a physical system should, and it\'s worth recording as a real number rather than a shape: target 0.5 m produced 0.202 m of overshoot at 0.42 m/s peak descent rate; target 1.0 m produced 0.307 m. Two boundary conditions, one honest instrument reading the same physical loop both times.',
    ],
  },
  {
    slug: 'what-thirty-minutes-of-sitting-still-taught-us',
    title: 'What thirty minutes of sitting still taught us about the IMU',
    dek: 'A vehicle at rest is the only condition outside water where the true state is known exactly. Using that fact properly meant verifying the rest itself, not just trusting the clock.',
    date: '2026-09',
    minutes: 6,
    thread: 'Estimation',
    tags: ['imu', 'characterization', 'sensors'],
    body: [
      'Underwater, truth is never directly available — that\'s the whole reason the estimation stack exists. But there is exactly one condition where it is: a vehicle sitting still on a bench, where the true velocity, true angular rate and true position are all known by definition. That measurement had never been taken on this hardware, so it became the first one.',
      '**89,813 samples over 1,797.0 seconds at 49.98 Hz** — and rest itself was verified rather than assumed: `|accel|` held at **9.7271 ± 0.0053 m/s²** for the full duration, confirming nothing bumped the bench and that what follows are sensor statistics, not desk statistics.',
      '## The headline, and the one that isn\'t',
      'Every gyro bias came back under **0.0004 °/s** — three orders of magnitude below the per-sample noise floor of roughly 0.09 °/s. That number has a direct architectural consequence: the board is already estimating and removing its own gyro bias before the message ever reaches `/mongla/imu`, so the localization filter must not try to estimate that same bias a second time — double-correcting a bias that\'s already near zero would only add noise. Residual yaw drift, measured over the same window, came out to **0.1° per hour**.',
      '> Allan deviation answers a different question than a standard deviation does, and it\'s the one that matters here.',
      'A plain standard deviation says how much one sample scatters around the mean. Allan deviation says how *long* the sensor can be averaged before bias drift starts dominating over noise reduction — which is the actual number a process-noise model should be tuned against. These gyros kept improving out to **660–860 seconds** of averaging, longer than any single mission leg this vehicle runs. Two numbers that looked like findings and weren\'t, logged specifically so they don\'t get "discovered" again later: the accel-x mean of −0.8718 m/s² is gravity on a tilted board mount, not a bias, and it\'s already accounted for elsewhere.',
    ],
  },
  {
    slug: 'the-tree-the-launcher-would-not-forward-signals-to',
    title: 'Killing a process tree the launcher wouldn\'t forward signals to',
    dek: 'Stopping the vehicle\'s software stack cleanly looked solved twice, on real hardware, and was wrong both times. A real test — not another hardware run — is what finally found the actual bug.',
    date: '2026-09',
    minutes: 6,
    thread: 'Reliability',
    tags: ['ros2', 'process-management', 'testing'],
    body: [
      'Killing `ros2 launch` orphaned every node it had started. Those orphaned nodes kept holding the Hailo-8, which surfaced an hour later — often in a completely different tool — as `HAILO_OUT_OF_PHYSICAL_DEVICES`, with no obvious connection back to the stop that had supposedly already happened.',
      '## Two fixes, both measured, both wrong',
      'The first attempt reset the inherited SIGINT-ignore state and signalled the launcher directly. It didn\'t exit within a 20-second window, so the escalation path sent SIGTERM — which `ros2 launch` does not forward to its children. They orphaned anyway. The second attempt tried `setsid` plus a process-group signal; `os.setsid()` failed in that execution context, the wrapper died immediately, and the stored PID pointed at a process that had never existed. The stop function returned success, having signalled nothing — silently. It printed neither "stopped" nor a failure. It just lied by omission.',
      'Both attempts shared the same broken assumption: that some other process in the chain would forward the signal onward. The actual fix drops that assumption entirely — `kill_tree` walks every descendant process and signals each one directly (INT, then TERM, then KILL as escalation), because `ros2 launch` starts each node as its own child, so the live process tree *is* the accurate node list, and nothing needs to relay anything.',
      '> Verified on hardware and correct is not the same claim as correct.',
      'A real test — not another hardware run — found a fourth defect the hardware genuinely could not have exposed: `kill_tree` re-walked the process list at every escalation step, which looks equivalent to walking once and isn\'t. The instant a parent process exits, its children are reparented to init, so a second walk can never find them again — any child that ignored SIGINT simply outlived its parent and was never signalled a second time. On real hardware this stayed invisible because ROS 2 nodes happen to die on the first SIGINT, so the underlying defect had zero opportunities to matter — until a test deliberately spawned children that ignore SIGINT on purpose, which is exactly the condition the tool exists for. The fix snapshots the whole tree *before* anything is asked to die.',
    ],
  },
  {
    slug: 'nobody-asked-what-the-camera-decided',
    title: 'Nobody had asked what the camera\'s own automatic loops were doing',
    dek: 'Every measurement this stack makes is computed on pixels — and every camera has automatic loops silently editing those pixels before any of them run. Reading the camera\'s own settings answered a question nobody had asked yet.',
    date: '2026-09',
    minutes: 7,
    thread: 'First principles',
    tags: ['cameras', 'calibration', 'measurement'],
    body: [
      'Detector confidence, XFeat inlier counts, a turbidity proxy, checkpoint bank references — every number this stack produces is computed on pixels, and until this pass, nobody had asked what the camera\'s own automatic control loops were doing to those pixels before any of them arrived. Read live off the running vehicle: `white_balance_automatic=1`, `auto_exposure=3` (Aperture Priority, not manual), `exposure_dynamic_framerate=1`. Nothing in the vision stack touches white balance at all, and dynamic framerate silently lets the camera drop its own rate in dim water — which matters specifically because every rung of the tracking lock ladder is sized in seconds: halve the frame rate, and you\'ve halved how many detections fit inside the same coast window without anyone changing a threshold.',
      '## The number that justified the question',
      'Measured on a static scene, with the camera itself perfectly still: auto-exposure injected **15.5× more luma drift than fixed exposure — 2.42 peak-to-peak against 0.16.** Every one of those descriptor-based measurements is intensity-derived, which means a reference image that never physically moved was still changing appearance from frame to frame, from a noise source none of the existing analysis accounted for.',
      '> The run also answered a different question than the one it was designed to ask.',
      'R/B — the red-to-blue channel ratio used as a colour proxy — came back at **exactly 1.0000, with zero variance, in both the auto and fixed arms.** That means the forward camera is delivering a monochrome signal, full stop, and automatic white balance is moot on it: there is nothing to balance. That result is consistent with a separate, already-standing finding that this specific camera needs a colour hardware variant — but it also means the white-balance half of the original question can\'t be settled on current hardware. Not disproven. Unfalsifiable, on this camera, today.',
      'Two honest limits close the finding rather than round it up. The 15.5× luma number comes from a static indoor scene — the easy case; a moving vehicle in changing water gives an auto-exposure loop far more to chase, and that harder version of the claim is still unmeasured. And the fixed-exposure arm itself read luma 59.55 against auto\'s 119.27 — under-exposed, because manual mode had inherited a stale `exposure_time_absolute=157` rather than computing the correct value. The fix that ships from this has to actively *set* the right exposure, not merely switch a loop off — and the function that already computes that correct value, `blur_capped_exposure()`, was sitting in the codebase, unused, the whole time.',
    ],
  },
];
